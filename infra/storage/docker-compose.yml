services:
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: easystack
      MINIO_ROOT_PASSWORD: easystack-secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data

  minio-init:
    image: minio/mc:latest
    depends_on:
      - minio
    environment:
      MINIO_ROOT_USER: easystack
      MINIO_ROOT_PASSWORD: easystack-secret
      STORAGE_S3_BUCKET: easystack
    volumes:
      - ./minio-init.sh:/minio-init.sh:ro
      - ./cors.json:/cors.json:ro
    entrypoint: ["/bin/sh", "/minio-init.sh"]

  storage-cdn:
    image: nginx:alpine
    depends_on:
      - minio
    ports:
      - "8081:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx-cache:/var/cache/nginx

volumes:
  minio-data:
  nginx-cache:
